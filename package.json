{
  "name": "invariant-packages",
  "private": true,
  "author": "Ben Newman <ben@apollographql.com>",
  "description": "Packages for working with invariant(condition, message) assertions",
  "license": "MIT",
  "keywords": [
    "invariant",
    "assertion",
    "precondition",
    "rollup",
    "plugin",
    "TypeScript",
    "development",
    "production",
    "NODE_ENV"
  ],
  "scripts": {
    "postinstall": "lerna exec -- npm install",
    "build": "lerna run build",
    "prepublish": "npm run build",
    "test": "npm run build && lerna run test"
  },
  "devDependencies": {
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.14.2",
    "lerna": "^3.13.4",
    "typescript": "^4.0.3"
  }
}
